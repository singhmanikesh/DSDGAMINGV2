[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Optional: cache node modules between builds
[cache]
  paths = ["node_modules"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
